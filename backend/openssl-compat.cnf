openssl_conf = openssl_init

[openssl_init]
ssl_conf = ssl_sect

[ssl_sect]
system_default = system_default_sect

[system_default_sect]
MinProtocol = TLSv1.2
# Excluir ML-KEM (post-quantum) — no soportado por Cloudflare R2
Groups = X25519:P-256:P-384:P-521:X448
